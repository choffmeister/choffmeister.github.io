extends ../resources/views/layout

block content
    each post in posts
        div.post
            div.post-header
                div.post-publishdate #{formatIsoDate(post.publishDate)}
                div.post-title
                    a(href="#{post.url}") #{post.title}
            div.abstract #{post.abstract}
